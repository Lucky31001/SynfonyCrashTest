<style>
  .catalog-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }
  .catalog-grid {
    margin-top: 50px;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 2rem;
  }
</style>
